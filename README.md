## ipush, a simple CLI and example set for Ionic Push

Push notifications can be hard.  `ipush` provides functionality to help change that.

## Usage

```bash
npm install
npm link
ipush init  # You'll be prompted for your info here.
```
